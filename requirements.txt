# Production dependencies for rescuetime-mcp
# These are the core dependencies needed to run the RescueTime MCP server

# FastMCP framework for MCP server implementation
fastmcp>=0.2.0

# HTTP client for API requests
httpx>=0.27.0

# Data validation and parsing
pydantic>=2.0.0

# Environment variable loading
python-dotenv>=1.0.0

# Structured logging
structlog>=23.1.0